{
  "name": "jutas",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "bundle:test": "bun run --cwd ./bundler test",
    "web:test": "bun run --cwd ./web test:unit",
    "test": "bun run web:test && bun run bundle:test",
    "build:web": "bun run test && bun run --cwd ./web build",
    "build": "bun index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
