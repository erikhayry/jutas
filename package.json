{
  "name": "jutas",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "bundle": "bun run --cwd ./bundler bundle",
    "bundle:test": "bun run --cwd ./bundler test",
    "web:test": "bun run --cwd ./web test:unit",
    "test": "bun run web:test && bun run bundle:test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
